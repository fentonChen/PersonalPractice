//temp.cc
#include <iostream>
#include <string>

using namespace std;

int main() {
  string fastIp;
  cout <<" fast ip is : " << fastIp.c_str() << endl;

  char a[46];
  //if (a[0] != '\0') {
  if (strlen(a) != 0)
    cout <<" strlen :" << strlen(a) <<endl;
  } else {
    cout <<" strlen is 0 " << endl;
  }
  return 0;
}
